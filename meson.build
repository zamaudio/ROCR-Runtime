project('hsa-runtime', 'cpp')

subdir('src')

#pkg_mod = import('pkgconfig')
#pkg_mod.generate(libraries : hsa-runtime,
#                 version : '1.0',
#                 name : 'hsa-runtime',
#                 filebase : 'hsa-runtime',
#                 description : 'AMD HSA Runtime.')
